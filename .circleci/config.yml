version: 2
jobs:
  build:
    docker:
      - image: circleci/node:10.15.3
    steps:
      - checkout
      - run: npm i
      - run: node server.js
workflows:
  version: 2
  build:
    jobs:
      - build
